"use client";
import {useEffect,useRef,useState} from "react";
export default function VideoInput(){const [mode,setMode]=useState<"camera"|"upload">("camera");const [file,setFile]=useState<File|null>(null);const [live,setLive]=useState(false);const [status,setStatus]=useState("READY");const ref=useRef<HTMLVideoElement>(null);const stream=useRef<MediaStream|null>(null);useEffect(()=>()=>stream.current?.getTracks().forEach(t=>t.stop()),[]);async function start(){try{const s=await navigator.mediaDevices.getUserMedia({video:true,audio:false});stream.current=s;if(ref.current){ref.current.srcObject=s;await ref.current.play()}setLive(true);setStatus("CAMERA LIVE")}catch{setStatus("CAMERA PERMISSION DENIED")}}function stop(){stream.current?.getTracks().forEach(t=>t.stop());stream.current=null;setLive(false);setStatus("STOPPED")}async function upload(){if(!file)return;const fd=new FormData();fd.append("file",file);setStatus("UPLOADING…");try{const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000"}/api/video/upload`,{method:"POST",body:fd});if(!r.ok)throw Error();setStatus("AI PROCESSING")}catch{setStatus("UPLOAD FAILED — CHECK API")}}return <div className="video-input"><div className="video-tabs"><button className={mode==="camera"?"selected":""} onClick={()=>setMode("camera")}>Phone / Webcam</button><button className={mode==="upload"?"selected":""} onClick={()=>setMode("upload")}>CCTV Video</button></div>{mode==="camera"?<><video ref={ref} muted playsInline className="video-preview"/><div className="video-actions"><span>● {status}</span>{live?<button onClick={stop}>Stop</button>:<button onClick={start}>Start camera</button>}</div></>:<><label className="dropzone"><input type="file" accept="video/*" onChange={e=>setFile(e.target.files?.[0]??null)}/><strong>{file?file.name:"Select surveillance footage"}</strong><span>MP4, WebM, MOV, AVI or MKV</span></label><div className="video-actions"><span>● {status}</span><button disabled={!file} onClick={upload}>Start analysis</button></div></>}</div>}
