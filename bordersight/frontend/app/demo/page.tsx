"use client";
import {useState} from "react";
const steps=['Initialize command center','Load surveillance scenario','Run AI detection','Simulate boundary crossing','Generate critical incident','Operator acknowledgement'];
export default function Demo(){const [step,setStep]=useState(0);const running=step<steps.length;return <main className="main standalone"><header className="topbar"><div><div className="eyebrow">BorderSight / SIH 2026</div><div className="title">Guided Demonstration</div><div className="subtitle">A deterministic review flow for the Phase-2 MVP.</div></div><a className="back" href="/">← Command Center</a></header><section className="demo-hero card"><div><div className="eyebrow">Scenario</div><h2>Restricted-zone intrusion</h2><p>Person approaches the virtual boundary, crosses into the protected area and triggers a high-confidence incident.</p></div><button className="demo-button" onClick={()=>setStep(running?step+1:0)}>{running?step===0?'Start demonstration':'Next step':'Run again'} →</button></section><section className="panel timeline">{steps.map((x,i)=><div className={`timeline-row ${i===step?'current':''} ${i<step?'done':''}`} key={x}><span>{i<step?'✓':i+1}</span><div><b>{x}</b><small>{i<step?'Completed':i===step?'In progress':'Waiting'}</small></div></div>)}</section><div className="demo-foot">Demo mode is presentation-safe: it uses authorized test footage/scenarios and keeps human verification in the operational loop.</div></main>}
